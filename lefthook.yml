pre-commit:
  parallel: true
  jobs:
    - name: govet
      run: go vet

    - name: lint
      run: golangci-lint run --timeout=5m

    - name: tests
      run: go test . ./database -v
      interactive: true

pre-push:
  jobs:
    - name: crap
      run: make crap
