pre-commit:
  parallel: true
  jobs:
    - name: govet
      run: go vet

    - name: tests
      run: go test . ./database -v
      interactive: true

pre-push:
  jobs:
    - name: crap
      run: make crap
